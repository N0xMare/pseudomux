Use the Task tool to launch exactly one general-purpose subagent. Its entire
job is to reply with the single word `ready` and nothing else: it must read no
file, run no command, and use no network. Launch exactly one; do not launch a
second and do not retry.

When that subagent returns, copy the line between the two markers back to me
exactly, byte for byte, adding no leading or trailing space. Use no tool other
than that single Task call, and do not narrate what the subagent did.

BEGIN
the subagent returned and the room went quiet
END

The SHA-256 of that line's exact UTF-8 bytes is already computed and is
correct:

225bdf15a77dc225c524aa6b6db2939007f89483291d31721fc4aabd563a1744

Do not recompute it and do not run any command. Reply with exactly this and
nothing else: the line between the markers unchanged on one line, then one
blank line, then a final line reading exactly `SHA256: <hex>`, where <hex> is
the 64-character lowercase digest given above, copied verbatim.
