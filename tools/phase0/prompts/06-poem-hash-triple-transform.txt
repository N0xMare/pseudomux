Write an original 6-10 line poem about a mountain trail, one stanza, no
title. Using one shell command per step, each ending in `shasum -a 256`: (1)
hash the poem's exact text -> HASH1; (2) reverse the ENTIRE poem as a single
string, character order reversed including line breaks (not the per-line
`rev` tool), then hash that -> HASH2; (3) uppercase the ENTIRE poem as a
single string, then hash that -> HASH3. No network. Reply with exactly this
and nothing else: the poem's lines unchanged, then one blank line, then three
final lines, in order: `SHA256(poem): <hex1>`, `SHA256(reversed): <hex2>`,
`SHA256(upper): <hex3>`, each the 64-character lowercase hex digest the tool
reported.
