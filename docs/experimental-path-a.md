# Interactive sessions (removed)

The product is [spec.md](spec.md): Messages + `run_stateless` + the thin
`pmux` CLI (`run` / `ping` / `doctor`) and the TypeScript, Rust, and Python
clients.

Interactive session commands and `claude-p` are not part of this product.
Current daemons refuse every session Request on the public wire. There is
no print-mode facade.

The historical recipe page is [archive/experimental-path-a.md](archive/experimental-path-a.md).
Do not copy those commands.
